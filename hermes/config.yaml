name: Hermes AI Bridge
slug: hermes
version: "1.0.6"
description: AI Bridge for Home Assistant with API access

startup: services
boot: auto
init: false

arch:
  - aarch64
  - amd64

host_network: true

ports:
  8080/tcp: 8080

options:
  hass_url: "http://homeassistant.local:8123"
  hass_token: ""

schema:
  hass_url: str
  hass_token: password